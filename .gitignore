# ── Bí mật & cấu hình cục bộ — KHÔNG BAO GIỜ commit ──
.env
.env.*
!.env.example

# CSDL SQLite (admin-auth.db chứa hash mật khẩu tài khoản) — không commit
*.db
*.sqlite
*.sqlite3

# ── Python ──
__pycache__/
*.pyc
.venv/
venv/

# ── Node / Vite (admin-web) ──
node_modules/
dist/
.vite/
*.tsbuildinfo

# ── Rác hệ điều hành / editor ──
*.log
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
